V. 1.0.3
---------

Features:
-Added feedback buttons that can appear on different place of front view: all results (below each Q&A), search results (below Q&A of result search only), any or none.


V. 1.0.2
---------

Languages:
-Added german package.

Bug fixed:
-When searching from URL /modules?r=faq, it did an error (module not found).


V. 1.0.1
---------

Bug fixed:
-When searching in FAQ page a word that is contained into a <a> tag, the yellow highlight HTML code was inserted into the href attribute. This caused a distorted output. Now nothing is allowed to be inserted between HTML tags.

Other:
-Correction in subject of suggestion's mail when no member connected.
